void function(int i, float x, string test, char c)
{
  if(a + b * c == b && b < 0)
    test;
  else if(tset)
    test;
  else if(test2)
    return true;
  else
    return;

  for(int i = 0;i<100;i = i + 1)
  {
  }
  while(i[10 + a]<0)
  {
  }
  i = test(-a+b,b,c,d,e);
}
